# `docs/` — internal engineering docs (not published)

## English
Internal documentation that is **not** part of the deployed site. Kept out of
`site/`, so it is never served publicly.

- `superpowers/` — the spec + implementation-plan artifacts produced by the
  brainstorm → spec → plan workflow (one pair per point).

## 中文
**不**属于已部署站点的内部文档。置于 `site/` 之外，故绝不公开提供。

- `superpowers/` — 由 头脑风暴 → 规格 → 计划 工作流产出的规格与实现计划制品（每个节点一对）。
