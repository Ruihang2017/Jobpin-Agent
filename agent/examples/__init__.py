"""Runnable examples for the agent core.

EN —
Small, self-contained scripts that demonstrate the core end-to-end. They default
to the offline ``FakeProvider`` so they run deterministically without an API key.

中文 —
演示内核端到端的小型自包含脚本。它们默认使用离线 ``FakeProvider``，无需 API 密钥即可确定性运行。
"""
