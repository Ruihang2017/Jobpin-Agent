"""Jobpin Agent — local-first, AI-agent HR platform (product source).

EN —
Top-level package for the Jobpin Agent product. It is intentionally a thin
placeholder today: the real internal structure (Agent Core, Memory Subsystem,
sub-agents, Layer B orchestration) is built incrementally through Phase 0 of
the Production Plan. The only public symbol right now is ``__version__``.
See ``../site/plan/02-Production-Plan-EN.md`` for the roadmap and
``../CLAUDE.md`` / ``CLAUDE.md`` for how development proceeds.

中文 —
Jobpin Agent 产品的顶层包。目前刻意保持为薄占位：真正的内部结构（Agent 内核、
记忆子系统、子代理、Layer B 编排）按生产计划 Phase 0 逐步构建。当前唯一对外符号是
``__version__``。路线图见 ``../site/plan/02-Production-Plan-EN.md``；开发方式见
``../CLAUDE.md`` 与 ``CLAUDE.md``。
"""

__version__ = "0.0.0"
