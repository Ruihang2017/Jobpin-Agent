# Superpowers subagent-driven-development scratch (ledger, task briefs, review packages)
.superpowers/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# Local secrets — never commit real keys (.env.example is the tracked template)
.env

# Local data stores + master key — never commit (§1.9: at-rest data + the OS-keystore key file)
*.db
*.sqlite
*.sqlite3
*.key

# Local run traces
agent/traces/

# Python (agent/)
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/
build/
dist/
